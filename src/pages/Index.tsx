const PlaceholderIndex = () => {
  return (
    <div className="flex min-h-screen items-center justify-center bg-stone-50 px-6">
      <div className="text-center">
        <h1 className="text-4xl font-semibold tracking-tight text-stone-900">SAAS App</h1>
        <p className="mt-3 text-base text-stone-600">Your application is ready for the next build step.</p>
      </div>
    </div>
  );
};

const Index = PlaceholderIndex;

export default Index;
